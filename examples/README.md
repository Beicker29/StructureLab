# Examples y Fixtures

`examples/case_0001` es el fixture oficial del repositorio para:

- pruebas automatizadas (`tests/`)
- smoke test (`scripts/smoke_api.py`)
- ejemplos en README

La app web usa `storage/jobs/*` como runtime principal.
